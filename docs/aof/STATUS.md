# ArchMap — AOF Shared Development State

> **Being phased out.** The canonical shared state is **[`.aof/`](../../.aof/)**
> (goals, tasks, context), managed via the AOF CLI. New handoff/triage goes into
> `.aof` task triage notes / `alignment-pulse`, **not** here. This file is kept
> for historical narrative only and is no longer updated per session.

Read `.aof` orientation/goals/tasks for current state. The notes below are a
historical bridge from the early Claude/Codex alternation.

- Frameworks: **AOF v6.5.0** (https://github.com/ai-org-labs/ai-organization-framework/tree/v6.5.0) · **QIF v0.3.0** (https://github.com/ai-org-labs/quality-intent-framework/tree/v0.3.0)
- Authoritative specs (in repo): [`docs/specs/v0.1/`](../specs/v0.1/) — authority order: product-principles → model-validation → dsl-syntax → views-rendering → engine-api
- Implemented-syntax reference: [`docs/SYNTAX.md`](../SYNTAX.md)

---

## Request framing (AOF)

- **Need:** the codebase predates the consolidated v0.1 spec set; align it to the specs.
- **Intent:** one canonical model drives base views (overview/zone/3d) + overlays (auth/dataflow/boundary/permission/validation); beautiful, readable rendering; trustworthy diagnostics.
- **Context:** working v0.1-ish impl exists (parser, layout w/ swimlanes + orthogonal routing, 6 views, opt-in icons via @archmap/icons, opt-in 3D). Spec introduces: canonical model as `Record`s, 4-level diagnostics `{level,code,message,ref,target}`, pair-key edges `A->B:`, edge id `${from}__${to}__${index}`, `boundaryCrossing` object, zone `parent`/nesting + cycles, `placement`, base-view/overlay split, expanded vocab + inference.

## Governance check (AOF — apply per task)

1. value/intent · 2. feasibility/execution · 3. risk/quality/safety. Run each task as **goal + execution + verification + stop-condition**. QIF: judge by quality-intent/risk/loss, not volume; separate **completion** (built+tested) from **success** (meets intent, verified).

---

## Implementation plan — order: parse → normalize → diagnostics → overview

Migrate incrementally; keep the product working and tests green at each step.

### Stage 1 — Parse (DSL → raw graph + metadata)  [task #10]
- Graph subset (dirs, 4 shapes, edges, subgraph, comments) — already present.
- **Add:** pair-key edges `A->B:` as selectors; explicit-id edges (from/to); generated edge id `${from}__${to}__${index}`; `edge_pair_ambiguous`.
- Completion: pair-key + explicit-id parse; generated ids; unit tests.
- Success: spec §6 edge identity rules hold on examples; no regression.

### Stage 2 — Normalize (→ canonical model)  [task #11]
- Canonical `Record`-keyed model (02 §4). Merge graph+metadata; `boundaryCrossing` object; data relation normalization (`edge.dataIds` ⇄ `data.flows`); zone `parent`/`resolvedContains`/nesting; `placement`; node `resolvedZone`; inference additions (metrics/logs/scan/vpn).
- Completion: canonical model shape produced; reference resolution; tests.
- Success: 02 normalization pipeline §2 reproduced; primary-zone resolution order correct.

### Stage 3 — Diagnostics (4-level)  [task #12]
- `{level,code,message,ref,target}`; `diagnostics` + derived `errors/warnings/suggestions/infos`; full code registry (02 §17); flow-sensitive auth (§18); boundary/zone/data/permission/vocab rules.
- Completion: all registry codes emitted where applicable; tests.
- Success: 02 acceptance §25 holds; warnings never block render.

### Stage 4 — Overview render (base view + overlay scaffolding)  [task #13]
- `render(model,{baseView,overlays})`; overview base view from canonical model; overlays as toggled projections (no reparse). Reuse existing layout/routing.
- Completion: baseView+overlays API; overview renders canonical model; overlays combine.
- Success: 03 acceptance §10 (overview + overlay combination) holds; legibility invariants kept (no edge over node, no shared endpoints).

Later (not this epic's first pass): zone view per 03 §3.2 (nested zones), 3D per 03 §3.3 (X=zone/Y=layer/Z=order), engine API (04), inspector/UI.

### Backlog reconciliation vs spec
- `#8` zone nesting → **core (in-scope)** via Stage 2/zone-view; node `contains` nesting stays backlog (spec §2.3).
- `#9` 4D/timeline → **v0.1 non-goal** (spec §9); keep parked.

---

## Decision log (AOF: Decision → rationale → outcome)

- 2026-06-26 — Adopt archmap-specs-v0.1 as authoritative; vendor into `docs/specs/v0.1/`. Implement in order parse→normalize→diagnostics→overview. Migrate incrementally (not a from-scratch rewrite) to protect the working product (QIF loss-boundary: unreadable/unusable output). Base/overlay split replaces the flat view registry over Stages 3–4.

---

## Handoff log (update at end of every session)

- 2026-06-28 — **Codex** — **AOF runtime upgraded to v6.5.0**. Installed local runtime cache at `/Users/mn/codex/.cache/aof-v6.5.0`, ran `aof upgrade --project .`, added `.aof/command-registry.json`, updated `.aof/project-bootstrap.json` / orientation routing to v6.5 command safety metadata, and switched current framework link to AOF v6.5.0. Verification: `aof --help --json`, `aof organization-verify --project .` (46/46 pass), `aof command-routing-audit --project .` (pass). Note: local Node is v25.5.0, so AOF emits its expected warning that CI validation is on Node 22.
- 2026-06-26 — **Claude** — Vendored specs, created this AOF state doc, set Stage tasks (#10–#13), reconciled #8/#9. Tests: 55 passing.
- 2026-06-26 — **Codex** — Stages 2–4 done (.aof TASK-001/002/003/004): canonical Record model (src/canonical.ts), 4-level diagnostics (src/diagnostics.ts), base-view+overlay render API, `<archmap-viewer>` element with `src` loading + diagnostics panel. Tests: 87.
- 2026-06-26 — **Claude** — Now operating via the **.aof runtime** (CLI: `node /Users/mn/codex/.cache/aof-v3.1.0/src/cli.js <cmd> --project .`). Implemented next-value-slice item **configurable console diagnostics** (spec 02 §23): `reportDiagnosticsToConsole(model, opts)` in src/diagnostics.ts, `render(model,{console})` opt-in, viewer `console` attribute (default on), exported from index. Tests: **92** (5 new). TASK-005 → done (engine viewer/src/diagnostics-UI/console complete). **Next: TASK-006** — viewer controls + SVG interaction (controls=true UI: base-view selector, overlay checkboxes, fit/reset, diagnostics indicator; wheel zoom, drag pan, responsive). `RenderResult.fit()/reset()` are still no-ops — wire them in TASK-006.
- 2026-06-26 — **Claude (Stage 1, historical)** — **Stage 1 (parse) DONE**. Added pair-key edge form `A->B:` (selector that enriches the matching graph edge), explicit-id form unchanged, generated graph-edge ids `${from}__${to}__${index}`, `edge_pair_ambiguous`, and edge fields `pairKey`/`source`/`graphLabel` on `ArchEdge` (types.ts). Impl: src/parser/metadata.ts. Tests: 58 passing (3 new pair-key tests in test/parser.test.ts). SYNTAX.md updated. **Next: Stage 2 (normalize)** — start with the canonical `Record`-keyed model shape and `boundaryCrossing` object + data-relation normalization (task #11). Note: diagnostics still use the OLD `{severity, ref:{kind,id}}` shape; the 4-level `{level,code,message,ref,target}` migration is Stage 3 (#12) — keep new diagnostics in the old shape until then, or coordinate the switch.
- 2026-06-27 — **Codex** — **Stage 2 (normalize) STARTED**. Added a Stage 2 normalization pass in `src/parser/metadata.ts` while preserving the existing array-based public model for render compatibility: `boundaryCrossing` now normalizes to `{crosses, reviewed, assertedFalse?}`; `edges.*.data` and `data.*.flows` normalize into `edge.dataIds` plus stable `data.flows`; pair-key flow refs resolve to generated/explicit edge IDs; zones/boundaries gain `resolvedContains`; nodes gain `resolvedZone`; parsed `placement`, zone `parent`/`owner`, and data `storage`. Updated boundary view + validation to use `resolvedZone` and canonical boundary crossing, including `zone_crossing_marked_false`. Tests: `npm run typecheck`, `npm test` (61 passing), `npm run build`. **Next:** continue Stage 2 with full canonical `Record`-keyed model shape or a compatibility adapter, zone parent/cycle/parent-contains diagnostics, `data_flow_mismatch`, and richer placement-derived zone inference.
- 2026-06-27 — **Codex** — **Stage 2 continued**. Added `toCanonicalModel()` (`src/canonical.ts`, exported from `src/index.ts`) to project the compatibility array model into the spec §4 Record-keyed canonical shape for AOF/runtime consumers without breaking existing renderers. Extended zone normalization with parent-derived child links, `zone_parent_unknown`, `zone_parent_conflict`, and `zone_cycle`; fixed zone/boundary validation to accept zone containment refs. Added `data_flow_mismatch` when edge-side `data` and data-side `flows` disagree before normalization. Tests added in `test/canonical.test.ts` and `test/parser.test.ts`. Tests: `npm run typecheck`, `npm test` (65 passing), `npm run build`. **Next:** Stage 2 remaining gaps are full source/subgraph preservation in canonical output, placement-derived zone inference, more exact unknown child-zone diagnostics, and then Stage 3 diagnostics shape migration.
- 2026-06-27 — **Codex** — **AOF runtime state initialized**. Created `.aof/` with AOF v3.1.0 `init --topology managed-project --install-mode runtime-on`, projected goals into `.aof/goals/`, and opened runtime tasks: `TASK-001` Stage 2 normalize, `TASK-002` Stage 4 overview overlays, `TASK-003` Stage 3 diagnostics. Replaced placeholder `.aof/context/active/project-orientation.json` content with ArchMap-specific boundaries, protected areas, required commands, verification entrypoints, and approval boundary. Verified with `aof organization-verify --project .` (22/22 pass) and `aof roadmap-status --project .`. **Operational change:** future Claude/Codex sessions should read `.aof/project-bootstrap.json`, `.aof/context/active/project-orientation.json`, `.aof/goals/*.json`, and `.aof/tasks/open/*.json` before this narrative STATUS to reduce repeated prompt/context cost.
- 2026-06-27 — **Codex** — **Stage 2 DONE / Stage 3 STARTED**. Completed `.aof` `TASK-001`: canonical output now preserves `source` and graph `subgraphs`; `toCanonicalModel()` returns Record-keyed collections plus source/subgraphs; `placement.zone` can infer `node.zone`/`resolvedZone` when it references a known zone; unknown zone containment distinguishes `zone_unknown_child_zone` from `zone_unknown_node`; edge inference now covers metrics/logs/security scan/vpn hints and expanded flow vocabulary. Started `.aof` `TASK-003` with diagnostics scaffolding: `ArchMapModel` now has `diagnostics`, `suggestions`, `infos`, and compatibility `level`/`target` fields derived from existing `errors`/`warnings`. Tests: `npm run typecheck`, `npm test` (69 passing), `npm run build`; `aof organization-verify --project .` remains 22/22 pass. **Next:** continue `TASK-003` by migrating diagnostic constructors to native `{level, code, message, ref, target}`, moving suggestion/info-level codes out of warnings, and covering the registry acceptance tests.
- 2026-06-27 — **Codex** — **Stage 3 advanced + dev server kept visible**. Started Vite dev server for continuous visibility; active URL for this run: `http://127.0.0.1:4174/` (verified by `lsof` listen; sandbox `curl` could not connect despite listener). Added `src/diagnostics.ts` registry normalization so `model.diagnostics` is the canonical combined list and level arrays are derived by code registry; `target` now uses spec-shaped `{type,id}` while legacy `ref` remains for compatibility. Validation view now reads `model.diagnostics`/`target`. Suggestion/info codes now land in `suggestions`/`infos` (`node_without_metadata`, `missing_direction`, `node_zone_unknown`, `data_without_classification`, `dataflow_missing_storage`, `placement_ref_unknown`, inference infos). Updated `docs/SYNTAX.md`. Tests: `npm run typecheck`, `npm test` (70 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** keep `TASK-003` open; migrate individual diagnostic constructors to native helpers and expand remaining registry coverage (auth issuer/validator refs, boundary zone/boundary-specific unknown refs, classification vocab, telemetry suggestions).
- 2026-06-27 — **Codex** — **Stage 3 registry coverage expanded**. Kept Vite visible at `http://127.0.0.1:4174/` (`lsof` confirms listener). Added native `diagnostic()` helper and used it for new diagnostics; parsed `auth.recipient`; added flow-sensitive auth coverage (`auth_flow_without_token`, token issue/validate issuer/validator/recipient rules, unknown auth refs), boundary coverage (`unknown_boundary_kind`, `boundary_unknown_related_zone`, boundary/node/zone-specific unknown contains refs, `boundary_cycle`), data coverage (`unknown_classification`, `data.processedBy` unknown nodes), and telemetry suggestion (`telemetry_without_data_classification`). Updated `docs/SYNTAX.md` registry list. Tests: `npm run typecheck`, `npm test` (74 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** `TASK-003` remains open for converting older parser/metadata/validate pushes to `diagnostic()` helpers and considering remaining vocab registries like zone/identity kind.
- 2026-06-27 — **Codex** — **Stage 3 helper migration advanced**. `validate.ts`, `parser/metadata.ts`, and `parser/graph.ts` now use `diagnostic()` helper instead of object-literal `{severity, ref}` pushes; added `STANDARD_ZONE_KINDS` and `STANDARD_IDENTITY_KINDS` plus `unknown_zone_kind` / `unknown_identity_kind` validation and tests. Vite remains visible at `http://127.0.0.1:4174/`. Tests: `npm run typecheck`, `npm test` (74 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** before closing `TASK-003`, decide whether renderer/view diagnostics (`unknown_base_view`, `unknown_overlay`, `view_3d_unavailable`, `src_fetch_failed`) belong here or should be deferred to `TASK-002` / engine API work.
- 2026-06-27 — **Codex** — **Stage 3 DONE / Stage 4 STARTED**. Closed `.aof` `TASK-003`: diagnostics now use the native helper path across parser/validate/render, sync to canonical `diagnostics` plus derived `errors`/`warnings`/`suggestions`/`infos`, and include view-facing codes (`unknown_base_view`, `unknown_overlay`, `view_3d_unavailable` registered). Started `.aof` `TASK-002`: `render(model,{baseView,overlays})` now coexists with legacy `render(model,{view})`; baseView selects the registered renderer, overlay names are validated without reparsing, known overlays are stamped onto the SVG root (`data-overlays`, `archmap-overlay-*`), and unknown overlays emit non-blocking diagnostics. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (76 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue `TASK-002` by turning the scaffold into real overlay projection composition on overview (auth/dataflow/boundary first, then validation/permission) while preserving legacy flat views.
- 2026-06-27 — **Codex** — **TASK-004 QIF spec alignment DONE; TASK-002 advanced**. Per human request, committed the prior AOF/Stage checkpoint first (`9701247`), then checked current implementation against `docs/specs/v0.1/` using QIF quality intent/risk/loss framing. Added [`docs/aof/QIF-SPEC-CHECK-2026-06-27.md`](QIF-SPEC-CHECK-2026-06-27.md). Fixed low-risk drift: `view.default: { base, overlays }` is honored; core now registers a `3d` fallback that emits `view_3d_unavailable`; `overview`/`zone` can compose semantic overlays without reparsing; flat `auth`/`dataflow`/`boundary`/`validation` views share the same projection logic; typed permission resources and `nodes.*.principal` principal resolution now match the model spec. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (80 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue `.aof` `TASK-002` with richer permission overlay edge synthesis and `RenderResult` overlay controls before custom-element/src engine work.
- 2026-06-27 — **Codex** — **TASK-002 advanced: permission overlay + RenderResult controls**. Permission overlay now synthesizes overlay annotation edges from principal-holder nodes to node resources, with role/action labels and resource badges. `RenderResult` now exposes `setBaseView(view)`, `setOverlays(list)`, `toggleOverlay(name)`, `fit()`, `reset()`, and `destroy()`; overlay/base-view updates rerender against the original target without reparsing. Updated `docs/SYNTAX.md`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (82 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** assess whether `.aof` `TASK-002` can close as Stage 4, or split custom element / external `src` / diagnostics target into a new engine API task.
- 2026-06-27 — **Codex** — **TASK-002 DONE / TASK-005 STARTED**. Closed Stage 4 `.aof` `TASK-002`: `render(model,{baseView,overlays})` and legacy `{view}` coexist; overview/zone render from the normalized model; auth/dataflow/boundary/permission/validation overlays combine without reparsing; permission overlay edges and `RenderResult` controls are covered. Updated QIF report residuals. Opened `.aof` `TASK-005` for `04-engine-api` work and implemented first slice: exported `defineArchMapViewerElement()`, `parseOverlaysAttribute()`, and `viewerOptionsFromAttributes()`; `initialize()` defines `<archmap-viewer>` by default; inline `<archmap-viewer base-view overlays width height>` renders inline source and updates base-view/overlays via `RenderResult` without reparsing. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (84 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue `TASK-005` with external `src`, `src_fetch_failed`, and diagnostics target/panel.
- 2026-06-27 — **Codex** — **v0.1 remaining task backlog cut**. Human asked whether all docs/specs tasks were cut; answer was no, so `.aof` now has the remaining v0.1 backlog as explicit open tasks: `TASK-005` Engine API (`<archmap-viewer>`, external `src`, `src_fetch_failed`, diagnostics UI), `TASK-006` viewer controls + SVG pan/zoom/responsive interaction, `TASK-007` inspector + diagnostics UX, `TASK-008` nested zones/boundaries + dense rendering quality, `TASK-009` 3D completion + overlay parity, `TASK-010` delivery/security/v0.1 acceptance matrix, and `TASK-011` recurring QIF spec alignment audit. `next-value-slice` now says to continue `TASK-005` first and keep `TASK-011` active at every task closure. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** implement `TASK-005` external `src` + diagnostics target/panel, then run/update QIF before closing it.
- 2026-06-27 — **Codex** — **TASK-005 advanced + QIF cadence applied**. Added external source/diagnostics engine support: `<archmap-viewer src>` now fetches external ArchMap source with `src` priority; failed loads emit `src_fetch_failed` and show diagnostics, using inline fallback only with `fallback-to-inline`; `render(model,{diagnosticsTarget})` plus viewer `diagnostics` / `diagnostics-target` render a diagnostics summary/list to an internal panel or external element. Exported `diagnosticsHtml()`, `renderDiagnostics()`, and `fetchArchMapSource()`. Updated `docs/SYNTAX.md` and the QIF report per `TASK-011`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (87 passing), `npm run build`. **Next:** add configurable console diagnostics and begin `TASK-006` controls/SVG interaction work.
- 2026-06-27 — **Codex** — **TASK-007 DONE**. Continued after Claude completed TASK-006. Added `src/inspector.ts` with `inspectModelElement()`, `inspectorHtml()`, and `renderInspector()` for node/edge/zone/boundary/data/permission/identity/diagnostic selections. `render(model,{inspectorTarget,selection})` and `<archmap-viewer inspector inspector-target>` now render selected-element details; viewer SVG clicks on nodes/edges/zones/boundaries/permission overlay edges update the inspector and emit `archmap:select-*` events with stable model refs. Diagnostics HTML now carries index/target data attributes for follow-up focus/filter/highlight. QIF report updated. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). Tests: `npm run typecheck`, `npm test` (96 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** TASK-008 nested zones/boundaries and dense rendering quality, including visual selected highlight and diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-012 interrupt DONE**. Human provided comprehensive sample v2 + QIF. Fixed spurious diagnostics by syncing standard vocabulary (`kubernetes_cluster`, `vpc_peering`, identity `user`/`external_user`, `telemetry_export`/`log_export`/`trace_export`), making auth validation flow-sensitive so `token_issue` does not require `validatedBy`, and narrowing `placement_ref_unknown` for standard placement dimensions. Actual sample parse via `dist/archmap.js`: 0 errors, 0 warnings, 0 suggestions, 2 expected `inferred_flow` infos. Tests: `npm run typecheck`, `npm test` (97 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** resume TASK-008.
- 2026-06-27 — **Codex** — **TASK-008 partial: label clearance**. Addressed browser comment on comprehensive sample where the OnPrem/On-premises zone label overlapped member nodes. Zone and boundary boxes now reserve a 36px top header area, and lane gap is widened to keep adjacent boxes separated. Added layout regression coverage; actual comprehensive sample `OnPremVPN.y - onprem.y` is now 36px. Tests: `npm run typecheck`, `npm test` (98 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue TASK-008 with nested zone/boundary rendering quality, selected highlight, diagnostics focus, and overlay conflict handoff.
- 2026-06-27 — **Codex** — **TASK-008 partial: hub component sizing**. Addressed browser comment on Cloud Run Service connection density. Layout now sizes components from connection density (graph edges plus permission overlay relationships), giving high-degree hubs more internal padding and port-distribution room without editing the DSL sample. Actual comprehensive sample: `CloudRun` now renders at hub size while nearby lower-degree nodes remain compact. Tests: `npm run typecheck`, `npm test` (100 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue TASK-008 with connection stacking/overlay conflict handoff and diagnostics focus.
- 2026-06-27 — **Codex** — **TASK-008 partial: permission overlay ports/labels**. Continued rendering-side fix only; DSL samples were not changed. Permission overlay edges now choose source/target sides from node geometry, distribute ports along each side, route orthogonally, and stack labels beside the target side instead of placing all labels at edge centers. Comprehensive sample permission overlay now emits orthogonal paths for synthesized permission edges. Tests: `npm run typecheck`, `npm test` (100 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue TASK-008 with broader edge-label collision handling and selected/diagnostics focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: dense permission label collapse**. Previous port routing still left long permission role labels/badges visible in dense diagrams. Dense permission overlays now suppress all long `roles/...` edge labels and resource badges, replacing them with short in-node summaries such as `1 permission` / `2 permissions`; details remain available via inspector. Actual comprehensive sample permission overlay now has 0 rendered `roles/...` texts and 9 permission summaries. Tests: `npm run typecheck`, `npm test` (101 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue remaining TASK-008 quality work.
- 2026-06-27 — **Codex** — **TASK-008 correction: high-degree edge side balancing**. Addressed remaining red-line concentration: normal graph edges incident to high-degree components now choose balanced component sides, not just the geometrically nearest side. Actual comprehensive sample `CloudRun` edge endpoints now distribute across left 4 / right 3 / top 4 / bottom 4. Added layout regression for multi-side hub routing. Tests: `npm run typecheck`, `npm test` (102 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. **Next:** continue non-permission label collision handling and selected/diagnostics focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: reciprocal edge tracks**. Addressed human-provided LR sample where `Web --> FirebaseAuth` and `FirebaseAuth --> Web` overlapped. Multi-edge pairs between the same two components now use side-aware outside tracks rather than the same H-V-H route; actual sample coordinates route `Web->FirebaseAuth` over the upper track and `FirebaseAuth->Web` over the lower track. Added regression coverage. Tests: `npm run typecheck`, `npm test` (103 passing), `npm run build`; Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 with broader non-permission label collision handling, selected highlight, and diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-008 partial: viewer taxonomy controls**. Updated viewer/playground terminology and settings around the product taxonomy: Views (`overview`, `zone`, `layer`), Render modes (`2d`, `isometric`, `3d`), and Overlays (`auth`, `dataflow`, `boundary`, `permission`, `validation`). Added registered `layer` semantic 2D view using layer-ranked layout, `renderMode`/`render-mode` plumbing, and checkbox overlays in dev/static demos. True isometric camera semantics remain TASK-009. Tests: `npm run typecheck`, `npm test` (104 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152).
- 2026-06-27 — **Codex** — **TASK-008 partial: movable isometric SVG**. Human clarified isometric should look like a station concourse map and still be movable. `renderMode: "isometric"` now renders a movable SVG 2.5D projection rather than routing to the 3D fallback: zones are floor polygons, nodes are shallow extruded boxes, and edges/overlays are projected into the same view. Existing SVG pan/zoom applies when rendered into a target. Tests: `npm run typecheck`, `npm test` (105 passing), `npm run build`; actual render string includes `archmap-render-isometric` and not the 3D fallback. **Next:** optional richer isometric controls (angle/depth/layer visibility) and continue TASK-008 collision/focus work.
- 2026-06-27 — **Codex** — **TASK-008 partial: station-map 3D styling**. Human asked whether angle can change and suggested the current isometric colors would work well in 3D. The optional Three.js 3D view already uses OrbitControls for angle changes; tuned it toward the station-map palette with pastel layer colors, softer translucent zones, white scene background, pale floor plane/grid, and a more isometric initial camera while retaining free rotation/pan/zoom. Tests: `npm run typecheck`, `npm test` (105 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152).
- 2026-06-27 — **Codex** — **TASK-008 correction: interactive isometric mode**. Human clarified they want to rotate the isometric view itself. Corrected `renderMode: "isometric"` to route to the optional Three.js renderer instead of static SVG, using `OrthographicCamera` + `OrbitControls`; dev playground has `installThreeView()`, so isometric can now rotate/pan/zoom with the station-map palette. Core fallback still reports 3D renderer unavailable when the optional view is not installed. Tests: `npm run typecheck`, targeted render/scene tests, `npm run build`.
- 2026-06-27 — **Codex** — **TASK-008 correction: additive overlay controls**. Human clarified checkboxes are ADD semantics: Overview is structure-only, checked overlays add information on top. Viewer controls now label overlays as `Add info` and use `addOverlay`/`removeOverlay` instead of blind toggle; render regression covers structural overview baseline plus additive auth/dataflow overlays. Tests: `npm run typecheck`, `npm test` (106 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass.
- 2026-06-27 — **Codex** — **TASK-008 correction: non-overlap routing priority**. Human clarified diagonal lines are acceptable, but overlapping endpoints/parallel bundles/label collisions are not. Dense hub and reciprocal side routes now use distributed ports with direct diagonal segments rather than orthogonal bundles; high-degree components can grow larger; label placement searches wide candidate positions and avoids node boxes, other labels, and edge segments. Actual comprehensive sample: `CloudRun` has 15/15 incident edges as direct 2-point routes, label-vs-node hits 0, label-vs-label hits 0. Tests: `npm run typecheck`, `npm test` (107 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass.
- 2026-06-27 — **Codex** — **TASK-008 correction: orthogonal component stubs**. Human clarified connectors must leave/enter components horizontally or vertically even if the middle segment is diagonal. Dense hub/reciprocal side routes now use orthogonal first/last stubs and a diagonal middle segment. Actual comprehensive sample: `CloudRun` 15/15 incident edges have horizontal/vertical start and end stubs, label-vs-node hits 0, label-vs-label hits 0. Tests: `npm run typecheck`, `npm test` (107 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass.
- 2026-06-27 — **Codex** — **TASK-008 correction: diagonal only as fallback**. Human clarified horizontal/vertical routing should be preserved wherever possible. Side routes now remain fully orthogonal unless the edge is reciprocal or a node face is overcrowded (4+ ports); diagonal is only used for the middle segment in those fallback cases, never at component boundaries. Added regression for non-overcrowded hub routes staying fully orthogonal. Actual comprehensive sample: `CloudRun` 15/15 incident edges have horizontal/vertical start/end stubs, 12/15 use diagonal middles due to overcrowded faces, label-vs-node hits 0, label-vs-label hits 0. Tests: `npm run typecheck`, `npm test` (108 passing), `npm run build`; `aof organization-verify --project .` 22/22 pass.
- 2026-06-27 — **Codex** — **TASK-008 correction: diagonal prohibited + endpoint dots**. Human clarified diagonal lines are prohibited. Removed diagonal fallback for normal graph routing: side routes now stay fully horizontal/vertical, distribute ports across component faces, and prefer longer/larger hub faces before consuming short faces. SVG rendering now places a small black dot at each edge/overlay endpoint. Actual comprehensive sample via `dist/archmap.js`: `edgeCount=40`, `diagonalEdges=0`, `CloudRun` incident `15/15` with `diagonalCloudRunEdges=0`, `label-vs-node hits=0`, `label-vs-label hits=0`, `endpointDots=59`. Tests: `npm run typecheck`, targeted layout/render tests (37 passing), `npm test` (109 passing), `npm run build`. AOF note: current installed `aof` CLI does not expose `organization-verify`; `aof --help` was checked, and runtime state was updated in `.aof/tasks/open/TASK-008.json`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 with selected highlight, diagnostics-to-diagram focus, and remaining nested boundary/zone visual polish.
- 2026-06-27 — **Codex** — **TASK-008 correction: start dots + shared parallel lanes**. Human corrected marker semantics: the small black dot belongs on the line start, not the end. Renamed/rendered startpoint dots for normal and overlay edges, removed endpoint dots, and changed SVG path construction so normal graph edges and overlay edges go through one shared parallel-lane allocator. Overlapping horizontal/vertical segments are offset together with orthogonal-only jogs; no diagonal segments are introduced. Actual comprehensive sample via `dist/archmap.js`: `renderedPaths=59`, `diagonalSvgSegments=0`, `parallelOverlaps=0`, `startDots=59`, `endpointDots=0`, `CloudRun` incident `15/15` still has `diagonalCloudRunLayoutEdges=0`. Tests: `npm run typecheck`, targeted edges/render tests (32 passing), `npm test` (110 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 selected highlight / diagnostics-to-diagram focus after visual confirmation.
- 2026-06-27 — **Codex** — **TASK-008 correction: component spacing**. Human flagged components as too close. Increased layout-level spacing rather than editing DSL samples: `RANK_GAP=170`, `NODE_GAP=72`, `LANE_GAP=128`, and added a same-lane separation regression. Actual comprehensive sample via `dist/archmap.js`: canvas `3620x3012`, `minNodeGap=72`, `diagonalSvgSegments=0`, `parallelOverlaps=0`, `startDots=59`, `endpointDots=0`. Browser current sample after reload: `minNodeGap=72`, `diagonalSegments=0`, `parallelOverlaps=0`. Tests: `npm run typecheck`, targeted layout/edges/render tests (47 passing), `npm test` (111 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue visual inspection and then selected highlight / diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: corner rendering**. Human screenshot showed broken-looking bend corners around CloudRun. Kept orthogonal-only routing but cleaned path rendering: crossing gaps are suppressed near segment corners, jog connectors return in the natural vertical-then-horizontal order, and edge strokes now use round line joins/caps. Actual comprehensive sample via `dist/archmap.js`: `renderedPaths=59`, `diagonalSvgSegments=0`, `parallelOverlaps=0`, rounded joins present. Browser current sample after reload: `lineJoin=round`, `lineCap=round`, `diagonalSegments=0`, `parallelOverlaps=0`. Tests: `npm run typecheck`, `npm test` (112 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue visual inspection, then selected highlight / diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: aligned offset corners**. Human identified the remaining bend issue: horizontal segments connecting corners were using different Y coordinates after per-segment offsets. `buildEdgePaths()` now first builds a route-level offset polyline, using the intersection of adjacent shifted horizontal/vertical lanes as the corner point. This keeps corner-to-corner horizontals on one Y coordinate while preserving orthogonal-only routing and shared parallel-lane offsets. Added regression for offset L-routes without interior tick marks. Browser current sample after reload: `diagonalSegments=0`, `parallelOverlaps=0`. Tests: `npm run typecheck`, targeted layout/edges/render tests (49 passing), `npm test` (113 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** visual confirmation on the highlighted CloudRun area.
- 2026-06-27 — **Codex** — **TASK-008 correction: protected component-exit stubs**. Human flagged routes that leave a startpoint and immediately travel back through their own component. `buildEdgePaths()` / `offsetPolyline()` now protects the first and last 14px of a route along the original component-face direction before applying parallel-lane offsets, so offset escaping cannot point into the source component. Added regression for first component-exit preservation. Tests: `npm run typecheck`, targeted `test/edges.test.ts test/render.test.ts test/layout.test.ts` (50 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 with remaining visual inspection, selected highlight, diagnostics-to-diagram focus, and nested boundary polish.
- 2026-06-27 — **Codex** — **TASK-008 correction: route bend count minimized**. Human clarified that line bend count should be as small as possible and 3+ points should be treated as an outlier. Layout routing now chooses complementary source/target faces so ordinary graph edges render as straight lines or one-bend L routes (`<=3` layout points), and SVG parallel-lane offsetting no longer adds extra protected stubs to simple `<=3`-point routes. Browser current sample after reload: `pathCount=11`, `maxPoints=3`, `over3=0`. Tests: `npm run typecheck`, targeted layout/edges/render tests (51 passing), `npm test` (115 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 with selected highlight, diagnostics-to-diagram focus, nested boundary polish, and a later explicit strategy if duplicate simple lines must be separated without adding bends.
- 2026-06-27 — **Codex** — **TASK-008 correction: non-rectangular shape boundary ports**. Human flagged start/end points on ellipse, diamond, and database shapes landing on the bounding box, causing lines to appear behind/inside shapes. Normal layout ports and overlay permission ports now project from the chosen face to the actual rendered shape boundary for circle/ellipse, diamond, and database cylinders. Startpoint dots now use the same color as the owning line, including emphasis and permission overlay strokes. Browser current sample after reload: `dotFill == pathStroke`, `pathCount=11`, `maxPoints=3`, `over3=0`. Tests: `npm run typecheck`, targeted layout/render tests (40 passing), `npm test` (116 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 visual inspection and selected highlight / diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: comprehensive fixture + no shape overlap**. Human re-supplied the large comprehensive sample and said it must be tested. Added `test/fixtures/comprehensive.archmap`; render tests now assert zone/boundary labels do not overlap in overview/layer with boundary overlay; layout tests assert graph edge samples never enter non-endpoint node shapes. Startpoint dots now have no white stroke. Routing still prefers straight/one-bend paths, but if a candidate intersects another shape it switches to an outer safety lane and evaluates source/target faces plus face slots against actual shape interiors; `>3` points are now allowed only for this no-shape-overlap exception. Browser current sample after reload: `dotFill == pathStroke`, `dotStroke=none`, `pathCount=11`, `maxPoints=5`, `over3=2`. Tests: `npm run typecheck`, `npm test` (118 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 visual inspection and selected highlight / diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-008 correction: canvas contains outer routed edges**. Human flagged edge lines clipped outside the canvas after outer safety lanes were introduced. `computeLayout()` now computes final extents after routing and label placement across nodes, zones, boundaries, edge points, and edge labels; when outer lanes produce negative or beyond-canvas coordinates, all geometry is shifted inside the `viewBox` and `width`/`height` are expanded. Comprehensive fixture test now asserts every edge point and label point is within the layout canvas. Browser current sample after reload: edge bounds fit within `viewBox`. Tests: `npm run typecheck`, `npm test` (119 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PID 59152). **Next:** continue TASK-008 visual inspection and selected highlight / diagnostics-to-diagram focus.
- 2026-06-27 — **Codex** — **TASK-011 sample audit checkpoint**. Imported the user-provided pattern sample set into `test/fixtures/pattern-samples/` and added `test/pattern-samples.test.ts` so all six samples must parse without errors/unexpected diagnostics and render through `overview`, `zone`, and `layer` with overlays. Added `docs/aof/PATTERN-SAMPLES-CHECK-2026-06-27.md`; QIF decision is to allow only the existing `zone_crossing_marked_false` warning for this fixture set and treat unknown vocabulary/render failures as blockers. Updated TASK-009 notes to use the samples for 3D/isometric acceptance. Tests: `npm run typecheck`, `npm test -- --run test/pattern-samples.test.ts` (12 passing), `npm test` (140 passing), `npm run build`. Vite remains visible at `http://127.0.0.1:4174/` (PIDs 1135, 4802, 59152).
- 2026-06-27 — **Codex** — **TASK-011 full pattern verification**. Human asked to verify every sample thoroughly. Added `tools/verify-pattern-samples.mjs` and `npm run verify:pattern-samples`: built `dist` check covers all 6 samples x 3 base views x 32 overlay sets = 576 2D renders, asserting diagnostics, SVG validity, orthogonal paths, startpoint parity, and viewBox containment. Added `tools/pattern-browser-verify.html`: live Vite/browser check covers 6 samples x 3 base views x 3 render modes with all overlays = 54 renders, including 18 SVG and 36 real isometric/3D canvas renders. Results: both verifiers passed with 0 failures. Vite remains visible at `http://127.0.0.1:4174/`.
- 2026-06-27 — **Codex** — **TASK-009 partial: 3D overlays + simplified controls**. Human requested the sidebar template include all Add info metadata, remove `zone` / `isometric` from visible controls, show icons in 3D, place zone labels to avoid top/front-view collisions, and apply Add info in 3D. Updated playground/static demo controls to `overview`/`layer` and `2d`/`3d`; enriched the playground seed with auth/dataflow/boundary/permission/validation metadata and no diagnostics; viewer controls now expose the same simplified taxonomy. 3D now consumes the shared overlay projection: emphasized edges, permission overlay edges, node badges, boundary volumes, provider/kind icons, and corner-offset zone labels. Tests: `npm run typecheck`, `npm test` (140 passing), `npm run build`, `npm run verify:pattern-samples` (576 renders, 0 failures), browser verifier (54 renders, 0 failures). Vite restarted and remains visible at `http://127.0.0.1:4174/` (PID 61553).
- 2026-07-02 — **Codex** — **TASK-077 DONE: static demo initial fit**. Browser review found `examples/demo.html` initial Overview rendering extremely small when opened via `file://`. Root cause was demo CSS applying `max-width: 100%` to the rendered ArchMap SVG while core pan/zoom fit also scales the SVG, causing double shrink. Scoped the CSS to `.diagram svg.archmap`, made it `display:block`, and removed responsive `max-width` so pan/zoom fit owns initial scaling. Tests: `npm run typecheck`, `npm test -- --run test/render.test.ts`, `npm run build`. **Next:** browser smoke on file/localhost/GitHub Pages, then npm publish/tag/jsDelivr verification for 0.1.1.
- 2026-07-02 — **Codex** — **TASK-078 DONE: Prototype map drag robustness**. Human reported the Prototype map area still could not be grabbed in the static demo. Strengthened map pan handling in `src/views/prototype.ts`: drag starts in capture phase on the flow pane, pointer movement is tracked on both pane and `window`, cleanup handles pointer/cancel/lost capture/window blur, and cards disable native text/image dragging. Tests: `npm run typecheck`, `npm test -- --run test/prototype.test.ts test/render.test.ts`, `npm run build`. Browser automation could not smoke-test `file://` because the in-app browser policy blocks file URL interaction. **Next:** manual file/localhost smoke; if still sticky, instrument live pointer events in localhost rather than file URL.
- 2026-07-02 — **Codex** — **TASK-079 DONE: localhost release sample sync**. Human asked whether the sample was set. `examples/demo.html` was already on the 0.1.1 lightweight checkout screenflow sample, but `index.html` (localhost dev playground) was still the older architecture sample and older toolbar/loading CSS. Synced `index.html` to the same release sample and UI behavior while keeping dev imports (`/src/index.ts`, `/src/views3d/three-view.ts`). Tests: `npm run typecheck`, seed parse/render check for both `index.html` and `examples/demo.html` across overview/layer/prototype with all overlays, `npm run build`. **Next:** reload `http://127.0.0.1:5173/` and manually smoke Prototype dragging.
- 2026-07-02 — **Codex** — **TASK-080 DONE: SVG export for 2D views**. Human clarified SVG export only needs to support `overview`/`layer`. Added `RenderResult.exportSvg()` and `RenderResult.downloadSvg()` for SVG-backed 2D views, and made mounted views such as `prototype`/`3d` reject SVG export explicitly. Updated `docs/SYNTAX.md` and `docs/specs/v0.1/04-engine-api.md`. Tests: `npm run typecheck`, `npm test -- render.test.ts`, `npm run build`. **Next:** decide whether a stream input API should be release-blocking or deferred behind a minimal buffered interface.
- 2026-07-02 — **Codex** — **TASK-081 DONE: buffered stream input API**. Human asked to implement stream support without adding UI. Added `createArchMapStream()` as a low-level buffered source interface exported from `@archmap/core`: `write(string|Uint8Array)`, `pipe(ReadableStream)`, `flush()`, `close()`, `abort()`, plus source/model/result getters. The API buffers source and reparses/rerenders the complete accumulated DSL at debounce/flush boundaries; it is not an incremental parser. Updated `docs/SYNTAX.md` and `docs/specs/v0.1/04-engine-api.md`. Tests: `npm run typecheck`, `npm test -- stream.test.ts render.test.ts`, `npm run build`. **Next:** manual browser smoke and release publish/tag verification.
